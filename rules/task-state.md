# Sub-Agent 页面任务状态

> 本文件用于记录并行原型生成任务的调度、完成和打回状态。

## 状态格式

```text
- [运行中] {页面名}.html
- [完成] {页面名}.html
- [打回] {页面名}.html（原因：{具体不符合验收标准之处}）
- [等待] {页面名}.html
```

## 更新规则

- 启动页面生成任务时，写入 `[运行中]`。
- 页面通过验收后，更新为 `[完成]`。
- 页面未通过验收时，更新为 `[打回]`，并写明具体原因。
- 尚未启动的页面，保持 `[等待]`。
- 同一页面任务最多重试 3 次，超过后上报用户决策。
